## Noise detector to monitor noise levels

- Evidently vibed, the only non-vibed part was the README 
- Uses silero VAD to detect speech, whisper to transcribe conversations (if any), and general "noise events" recording >= 15db (you can change the param)